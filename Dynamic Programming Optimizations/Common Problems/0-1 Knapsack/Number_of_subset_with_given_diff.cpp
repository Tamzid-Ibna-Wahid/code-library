#include <bits/stdc++.h> 
using namespace std;
typedef long long ll;
 
int main(){
    int n,dif;
    cin>>n>>dif;
    vector<int>v(n);

    for(int i=0;i<n;i++)cin>>v[i];

    // s1 - s2 = dif
    // s1 + s2 = total
    // s1 = (diff + total) / 2;

    // now count number of way to create sum is equal to s1

    return 0;
}